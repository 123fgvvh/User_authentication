<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <title>OTP Verification</title>
</head>
<body class="bg-gray-100 h-screen flex items-center justify-center">

    <div class="bg-white p-8 rounded shadow-md w-full max-w-md">

        <h2 class="text-2xl font-bold mb-4">Verify OTP</h2>

        <!-- OTP Verification Form -->
        <form method="post" action="/verify_otp/<%=email%>">
            <!-- OTP Input -->
            <div class="mb-4">
                <label for="otp" class="block text-gray-600 text-sm font-semibold mb-2">Enter OTP:</label>
                <input type="text" id="otp" name="otp" class="w-full px-3 py-2 border rounded-md focus:outline-none focus:border-blue-500" required>
            </div>

            <!-- Submit Button -->
            <button type="submit" class="w-full bg-blue-500 text-white py-2 rounded-md hover:bg-blue-600 focus:outline-none focus:ring focus:border-blue-300">Verify</button>
        </form>

        <!-- Resend OTP Link -->
        <div class="mt-4">
            <p class="text-gray-600 text-sm">Didn't receive the OTP? <a href="#" class="text-blue-500">Resend OTP</a></p>
        </div>

        <!-- Back to Login Link -->
        <div class="mt-4">
            <p class="text-gray-600 text-sm">Remember your password? <a href="#" class="text-blue-500">Login</a></p>
        </div>
    </div>

</body>
</html>
